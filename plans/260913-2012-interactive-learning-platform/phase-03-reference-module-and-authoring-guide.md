# Phase 03 — Reference module M01 + authoring guide

**Priority:** P0 · **Status:** pending · **Depends on:** 02

## Goal
Lead writes M01 (Linux & Shell) fully → gold-standard example; `docs/content-authoring-guide.md` codifies format so parallel agents produce consistent content.

## Lesson template (every lesson)
1. `<Eli5>` — everyday analogy (Vietnamese daily life), no jargon
2. Illustration — ≥1 interactive SVG diagram per lesson
3. "Nói kỹ thuật hơn" — precise explanation, English terms kept
4. `<TerminalBlock>` hands-on commands with expected output
5. `<Callout type="mistake">` common mistakes
6. `<KeyTerms>` recap + `<QuickCheck>`

## Success criteria
- M01 renders, diagrams interactive in light/dark, typecheck passes
- Guide includes checklist + registry wiring instructions
